services:
  edge:
    image: placeos/edge:latest
    container_name: edge
    restart: always
    environment:
      - PLACE_URI=https://<PLACEOS DOMAIN>
      - PLACE_EDGE_KEY=<YOUR EDGE KEY>
    logging:
      driver: json-file
      options:
        max-size: 99m
